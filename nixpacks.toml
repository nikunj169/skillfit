[phases.setup]
aptPkgs = ["libglib2.0-0", "libgl1", "libxcb1"]

[phases.install]
cmds = ["pip install --no-cache-dir -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
