SkillFit Change Log
===================

Implemented before this log:
- Set up the Vite frontend scaffold with React entry files, root scripts, and a working build.
- Created a FastAPI backend starter with SQLite-friendly defaults, starter models, routers, tests, Docker config, and infra placeholders.
- Added repo-level helper scripts and a `.gitignore` for generated assets and local environments.

2026-05-02
- Started the next implementation phase after reviewing the README thoroughly.
- Chosen next milestone: build the end-to-end candidate/admin frontend flow and align backend endpoints with the documented API.
- Added a README implementation status section so the repository now records what is already working and what is still placeholder/starter behavior.
- Expanded the backend interview flow with documented endpoints for question fetch, finalize, session status, admin login, admin stats, and candidate status updates.
- Added a starter question bank service and workforce-category inference to support role-based interview sessions.
- Reworked the frontend into a routed candidate/admin application with contexts, hooks, services, pages, reusable UI components, and production-buildable styling.
- Wired the candidate flow from landing to registration, permissions, interview, and completion screens using the live backend API.
- Wired the admin flow from login to dashboard, candidate list, stats, filtering, and detail view using the live backend API.
- Filled the i18n files with starter locale content and initialized the translation layer.
- Extended backend tests to cover finalize/status and admin login/stats, then verified the suite passes.
- Verified the frontend production build passes after the new app wiring.
- Implemented the next small step after a fresh README review: moved the question-bank workflow closer to the documented design.
- Added a `questions` database model and made the interview question endpoint prefer DB-backed question rows when they exist.
- Kept a fallback in the in-code question bank so local development does not break if the database has not been seeded yet.
- Replaced `scripts/seed_questions.py` placeholder logic with a working seed script for starter electrician and delivery associate question sets in English, Hindi, and Kannada.
- Added a small backend test to cover the question-fetch endpoint.
- Updated the README to reflect the new question-bank persistence behavior and the current fallback approach.
- Implemented the next small README-aligned step: enriched the admin candidate detail flow with the latest transcript and assessment summary.
- Expanded the admin candidate detail API to include the latest stored session transcript, session status, and latest assessment score breakdown.
- Updated the admin candidate detail page to render transcript text plus strengths and improvement areas from the latest assessment.
- Added a focused backend test to verify the admin detail response now includes transcript and assessment data after interview finalization.
- Updated the README to reflect the richer admin review experience and its current scope.
- After a full README and repo-wide audit, implemented the next smallest meaningful candidate-flow step: a dedicated processing/status screen before final results.
- Updated the candidate interview flow to navigate to a processing route after the last answer instead of jumping directly to the completion page.
- Reused the existing session-status endpoint on the processing screen so the frontend now reflects the documented processing step in the README.
- Added a loading indicator plus basic failure messaging on the processing page.
- Updated the README to describe the new processing/status route and to clarify that backend finalization is still synchronous today.
- After another full README and repo-wide audit, implemented the next single logical step: made interview finalization use a lightweight backend background-processing flow.
- Updated the finalize endpoint so it marks sessions as `PROCESSING` and offloads assessment/classification updates to a background task.
- Updated the candidate processing page to poll the status endpoint until the session reaches `COMPLETED` or `FAILED`.
- Adjusted interview status responses to provide the final next-step message needed by the result screen.
- Updated the README to reflect that finalization is no longer purely synchronous, while noting that this is still a lightweight in-process background model rather than a full queue system.
- After another full README and repo-wide audit, implemented the next single logical step: persist per-question interview responses and surface them in admin review.
- Added a `question_responses` model and now store each submitted answer with its question key, question text, transcript, and order index.
- Expanded the admin candidate detail API to include `response_history` for the latest session.
- Updated the admin candidate detail page to render the per-question responses under the latest transcript and assessment summary.
- Updated the README to reflect that per-question transcripts now persist, while per-question scoring is still not stored yet.
- After reviewing the repository again, realized the `VideoRecorder` had been updated to send real video blobs, and per-question scoring was already implemented.
- Chosen the single most logical next step: Wire up the integrity placeholders (audio, face, duplicate detection) into the async pipeline.
- Added `face_presence_ratio` to the `Candidate` model and schema.
- Updated `backend/tasks/async_pipeline.py` to invoke the mock integrity validators and compute average face presence.
- Updated `CandidateDetail.jsx` and the admin router to pass and render the `face_presence_ratio`.
- Updated `README.md` to reflect that video blobs are now submitted, per-question scoring is fully persisted, and integrity checks are wired into the classification pipeline.
- Implemented the next logical step: Replaced the mock Face Validation logic with real MediaPipe/OpenCV ML processing.
- Added `opencv-python-headless` and `mediapipe` to `backend/requirements.txt`.
- Updated `backend/services/integrity/face_validator.py` to extract frames from `.webm` blobs at ~2fps and run MediaPipe Face Detection to accurately compute the `face_presence_ratio`.
- Implemented the next logical step: Replaced the mock LLM Assessment logic with real OpenAI API integration.
- Added `openai` to `backend/requirements.txt` and updated `backend/config.py` with `llm_provider` and `anthropic_api_key`.
- Updated `backend/services/assessment/llm_assessor.py` to prompt `gpt-4o-mini` with strict JSON-mode for evaluating candidate transcripts on relevance, completeness, clarity, and confidence.
- Implemented the next logical step: Integrated browser-native Text-to-Speech (TTS) for question delivery.
- Updated `frontend/src/components/AIVoicePrompt.jsx` to use `window.speechSynthesis`, allowing the candidate to actually hear the AI voice read the questions in English, Hindi, or Kannada.
- Implemented the next logical step: Wired up the Admin Action buttons for shortlisting candidates.
- Updated `frontend/src/pages/admin/CandidateDetail.jsx` to consume the `updateCandidateStatus` service, adding an interactive button that allows admins to officially shortlist candidates.
- Implemented the next logical step: Added pagination and sorting to the Admin Dashboard candidate list.
- Updated `frontend/src/hooks/useAdminFilters.js` and `frontend/src/pages/admin/Dashboard.jsx` to support dynamic column sorting and paginated views, completing the Admin Dashboard MVP requirements.
- Implemented the next logical step: Added Language and Role dropdown selectors to the Candidate Registration flow.
- Updated `frontend/src/pages/candidate/Registration.jsx` to expose the backend's multilingual and role-specific capabilities to the candidate UI, replacing the free-form text inputs with structured `<select>` elements.
- Implemented the next logical step: Replaced the mock Duplicate Detection logic with a functional SQLite-compatible cosine similarity search.
- Updated `backend/services/integrity/duplicate_detector.py` to query all existing `CandidateEmbedding` vectors and compute pairwise cosine similarity, flagging matches above 0.85.
- Updated `backend/tasks/async_pipeline.py` to generate a deterministic 128-dim embedding from the candidate name, persist it to the `CandidateEmbedding` table, and pass both the vector and db session to the detector.
- Implemented the next logical step: Replaced the mock Audio Validation logic with real ffmpeg-based SNR and silence analysis.
- Updated `backend/services/integrity/audio_validator.py` to extract audio via ffmpeg subprocess, compute RMS-based SNR and silence ratio from raw PCM samples, and gracefully fall back to mock values when ffmpeg is not installed on the host.
- Implemented the next logical step: Fixed the broken Integrity Router and updated the test suite.
- Updated `backend/routers/integrity.py` to pass required arguments (video_path, candidate_id, db) to the updated service functions via query parameters and dependency injection.
- Updated `backend/tests/test_integrity.py` to match the new API contract, adding a test that creates a real candidate and verifies duplicate detection against the database.
- Implemented the next logical step: Wired the LLM_PROVIDER=claude configuration so the assessment engine uses Anthropic Claude when configured.
- Added `anthropic>=0.40.0` to `backend/requirements.txt`.
- Refactored `backend/services/assessment/llm_assessor.py` into small helpers and updated `assess_question_response` to route Claude → OpenAI → heuristic based on the `llm_provider` setting; fixed `assess_transcript` (the standalone endpoint) to delegate through the same routing path.
- Completed `.env.example` with all documented API keys using the SKILLFIT_ prefix that config.py expects.
- Implemented the next logical step: Fixed the PermissionGate to be a real gate.
- Replaced the duplicate and misleading navigation buttons with a single flow: clicking "Allow Camera and Mic" triggers `requestPermissions`, and a `useEffect` auto-navigates to `/interview` when `permissionState === "granted"`.
- Added a contextual error message and "Try Again" button for the `permissionState === "denied"` case.
- Users can no longer skip past the gate; the only forward path requires the browser permission grant to succeed.
- Implemented the next logical step: Updated README.md to match actual codebase state.
- Corrected the "Current Scope of the Prototype" bullet that still said "integrated with the OpenAI API" — now accurately states Claude/OpenAI routing via SKILLFIT_LLM_PROVIDER.
- Removed the stale database schema note claiming per-question scoring fields are not stored; replaced with accurate description of what is persisted and surfaced.
- Added two "Completed So Far" bullets documenting the configurable LLM provider and the real PermissionGate, which were implemented but never recorded in the README.
- Implemented the next logical step: Localized the InterviewComplete result screen.
- Added a `complete` section to all three i18n files (en.json, hi.json, kn.json) with localized strings for the eyebrow, headline, no-result state, score cards, buttons, all five fitment label display names, and per-label next-step messages.
- Updated `FitmentBadge.jsx` to accept an optional `displayLabel` prop so the raw enum label continues to drive the CSS class while the localized human-readable label is shown to the candidate.
- Rewrote `InterviewComplete.jsx` to use `useTranslation()` — all hardcoded English strings are now i18n keys, the fitment badge shows the localized label (e.g., "ಕೆಲಸಕ್ಕೆ ಸಿದ್ಧ" for Kannada JOB_READY), and the next-step message is derived from the fitment label via i18n rather than the backend's hardcoded English string.
- Implemented the next logical step: Localized the Registration page.
- Added a `registration` section to all three i18n files (en.json, hi.json, kn.json) covering the eyebrow, headline, all five field labels, all four role option display names, and Back/Continue buttons.
- Rewrote `Registration.jsx` to use `useTranslation()` — role options render localized display text while keeping English values so backend `normalize_role` is unaffected; changing the language dropdown also calls `i18n.changeLanguage()` so the rest of the UI switches immediately, matching the pattern established in Landing.jsx.
- Implemented the next logical step: Localized the PermissionGate page.
- Added a `permissions` section to all three i18n files (en.json, hi.json, kn.json) covering the eyebrow, headline, body paragraph, denied-state error message, Back button, and both primary button states (Allow Camera and Mic / Try Again).
- Rewrote `PermissionGate.jsx` to use `useTranslation()` — all hardcoded English strings replaced with `t()` calls; all logic (auto-navigate on grant, conditional error display, disabled state, button text toggle on denied) is unchanged.
- Implemented the next logical step: Localized the Interview page.
- Added an `interview` section to all three i18n files (en.json, hi.json, kn.json) covering the eyebrow, h1 headline with `{{role}}` interpolation, recording-saved status, loading-questions state, Exit button, and both submit button states (Submit and Continue / Submit and Finish).
- Rewrote `Interview.jsx` to use `useTranslation()` — derives `localizedRole` by reusing existing `registration.roles.*` keys so Kannada users see e.g. "ವಿದ್ಯುತ್ ತಂತ್ರಜ್ಞ ಸಂದರ್ಶನ" rather than "Electrician ಸಂದರ್ಶನ"; all handlers, hooks, and component logic are unchanged.
- Implemented the next logical step: Localized the Processing page, completing full i18n coverage of the candidate flow.
- Added a `processing` section to all three i18n files (en.json, hi.json, kn.json) covering the eyebrow, headline, all four status messages (submitting, finalizing, checking, error), and the footer note.
- Rewrote `Processing.jsx` to use `useTranslation()` — `useState` initial value uses a lazy initializer `() => t(...)` so the first render is already localized; all three `setStatusMessage` calls inside the `useEffect` use `t()` calls; `t` is added to the `useEffect` dependency array (safe because the `hasStartedRef` guard prevents re-execution on re-render); all JSX strings replaced with `t()` calls.
- Implemented the next logical step: Fixed two specification bugs in the fitment classifier that caused incorrect candidate outcomes.
- Corrected the `else` branch in `fitment_classifier.py` from `LOW_QUALITY_SUBMISSION` to `REQUIRES_MANUAL_VERIFICATION`: a candidate who gives weak answers but has no integrity issues (no bad audio/video, not a duplicate) should be flagged for manual human review, not told their submission was technically defective.
- Aligned the classification thresholds with the README spec: `JOB_READY` threshold changed from `>= 8` to `>= 7.5` (matching README's 0.75 on a 0–1 scale), and `REQUIRES_UPSKILLING` threshold changed from `>= 6` to `>= 4.5` (matching README's 0.45); both fixes applied to the 0–10 scoring scale the code uses internally.
- Rewrote `tests/test_classification.py` to cover the corrected logic with boundary tests at 7.5, 4.5, and below, and explicit tests confirming that `LOW_QUALITY_SUBMISSION` is driven by an integrity flag (not a score fallback) and that `REQUIRES_MANUAL_VERIFICATION` is the correct label for a low-scoring clean submission.
- Implemented the next logical step: Added Plumber and Welder question sets to the in-code question bank, closing a silent content bug where all four registration roles were available but only two had questions.
- Added `("plumber", "en/hi/kn")` and `("welder", "en/hi/kn")` entries to `QUESTION_BANK` in `backend/services/questions.py` with 3 role-appropriate, trade-specific questions per language; Hindi questions follow the natural Hinglish register used by the existing delivery_associate and electrician sets; Kannada questions are in formal Kannada consistent with existing entries.
- The seed script (`scripts/seed_questions.py`) automatically picks up the new entries since it calls `get_question_seed_rows()` which iterates over `QUESTION_BANK` — no changes needed there.
- The `infer_workforce_category` function already mapped both "plumber" and "welder" to `BLUE_COLLAR_TRADE` — no changes needed there either.
- Added three new tests to `backend/tests/test_asr.py`: English plumber question fetch (verifying plumbing-specific vocabulary), Kannada welder question fetch (verifying Kannada script is present), and a Plumber session start test confirming the first question is not an electrician question.
- Implemented the next logical step: Added four distinct admin action buttons (Shortlist for Job, Shortlist for Training, Flag for Review, Reject), matching the Demo Flow and Core Modules spec in the README.
- Replaced the boolean `ShortlistRequest` schema in `backend/schemas/admin.py` with `CandidateActionRequest` carrying a `Literal["shortlist_job", "shortlist_training", "flag_review", "reject"]` action field.
- Added `_ACTION_MAP` dict in `backend/routers/admin.py` that maps each action to `(shortlisted: bool, status: str)`; the PATCH endpoint now derives both fields from the action rather than computing status from a boolean.
- Updated `frontend/src/services/admin.service.js` so `updateCandidateStatus(candidateId, action)` sends `{ action }` instead of `{ shortlisted }`.
- Replaced the single Shortlist/Unshortlist toggle in `frontend/src/pages/admin/CandidateDetail.jsx` with four labelled buttons; each button highlights (button-primary) when `candidate.status` matches its corresponding outcome, so the active action is visually clear.
- Updated `frontend/src/pages/admin/Dashboard.jsx` so the inline quick-shortlist button passes the current `shortlisted` state (not the inverted value) to `handleShortlist`, which maps to `shortlist_job` or `flag_review` accordingly.
- Added `test_admin_candidate_actions` to `backend/tests/test_admin.py` which verifies all four actions produce the correct `(status, shortlisted)` pair on the same candidate in sequence.
- Implemented the next logical step: Made admin credentials configurable via environment variables and turned `seed_admin.py` into a real setup script.
- Added `admin_username` and `admin_password` fields to `backend/config.py` (defaults: `admin@skillfit.in` / `skillfit2024`), both readable via `SKILLFIT_ADMIN_USERNAME` and `SKILLFIT_ADMIN_PASSWORD` env vars.
- Updated `backend/routers/admin.py` to read credentials from `settings.admin_username` and `settings.admin_password` instead of hardcoded literals — credentials can now be changed without editing source.
- Replaced the one-line `scripts/seed_admin.py` placeholder with a working script that loads the active settings, prints the effective credentials (username, password, token), and warns when all values are still at their insecure dev defaults.
- Added `SKILLFIT_ADMIN_USERNAME` and `SKILLFIT_ADMIN_PASSWORD` to `.env.example` under the Admin section.
- Updated the README "Completed So Far" to reflect the 4-action admin implementation and the working seed_admin script; corrected "Known Limitations — Role coverage" to list Welder (added) and remove Retail Staff (never added).
- Implemented the next logical step: Localized the AIVoicePrompt component, completing full i18n coverage of the candidate-facing UI.
- Added an `aiVoice` section to all three i18n files (en.json, hi.json, kn.json) with localized strings for the section kicker ("Interview Question" / "साक्षात्कार प्रश्न" / "ಸಂದರ್ಶನ ಪ್ರಶ್ನೆ"), the play button ("Play Question" / "प्रश्न सुनें" / "ಪ್ರಶ್ನೆ ಕೇಳಿ"), and the stop button ("Stop Audio" / "रोकें" / "ನಿಲ್ಲಿಸಿ").
- Updated `frontend/src/components/AIVoicePrompt.jsx` to import `useTranslation` and replace all three hardcoded English strings with `t()` calls; the TTS logic (language mapping, Web Speech API, isPlaying state) is unchanged.
- Implemented the next logical step: Replaced the dead-code `prompt_templates.py` stub with real per-role and per-language prompt context, and wired it into the LLM assessment engine.
- Rewrote `backend/services/assessment/prompt_templates.py` with `get_role_context(role_applied)` and `get_language_note(language)` — four role-specific evaluation criteria (electrician, delivery_associate, plumber, welder) covering the exact skills asked in each question set, plus three language-specific assessment notes (Kannada Kanglish tolerance, Hindi Hinglish tolerance, English non-native tolerance).
- Updated `backend/services/assessment/llm_assessor.py`: replaced the `build_assessment_prompt` import with `get_role_context, get_language_note`; updated `_build_prompts()` to incorporate role context and language note into the system prompt; removed the dead `build_assessment_prompt(role_applied, language)` call from `assess_transcript()` whose return value was previously discarded.
